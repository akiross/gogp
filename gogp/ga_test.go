package gogp

import "testing"

func TestReverse(t *testing.T) {
	/*
		cases := []struct {
			given, desired string
		}{
			{"Hello, World!", "!dlroW ,olleH"},
			{"", ""},
			{"Ciao Belli", "illeB oaiC"},
		}

		for _, c := range cases {
			got := Reverse(c.given)
			if got != c.desired {
				t.Errorf("Reverse(%q) == %q, want %q", c.given, got, c.desired)
			}
		}
	*/
}
